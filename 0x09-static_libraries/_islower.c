#include "main.h"
#include<stdio.h>

/**
 * main-checks if lowercase
 * if lower , return 1 
 * else return 0
*/

int _islower(int c)
{

if((c <= 'z') && (c <='a'))

{
	return 1; 
}
else
{
	return 0;

}
		}
