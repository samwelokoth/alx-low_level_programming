#include <stdio.h>

/**
 * @brief Prints a string followed by a newline.
 *
 * This function prints the given string followed by a newline character.
 *
 * @return Always 0 (Success).
 */
int main(void)
{
    puts("\"Programming is like building a multilingual puzzle\n");
    return (0);
}
