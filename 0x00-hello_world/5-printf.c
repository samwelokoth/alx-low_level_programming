#include <stdio.h>

/**
 * main - Entry point of the program.
 *
 * Description: This program prints a specific text to the standard output.
 * Use the function printf to achieve this.
 *
 * Return: Always 0 (Success).
 */
int main(void)
{
    printf("with proper grammar, but the outcome is a piece of art,\n");
    /* The main part of the program */

    return (0); /* If success */
}

